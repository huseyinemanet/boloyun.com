# Egress rollout — 18 September 2026

The owner authorized application of the prepared fixes after the initial audit.
This record distinguishes changes already active from the pending application/DB rollout.

## Active: Nginx perimeter (19:10 UTC)

- Backed up the previous site configuration under `/opt/boloyun/backups/egress-20260918`.
- Installed the HTTP-context `boloyun-egress.conf` and updated BOLOyun site config.
- `nginx -t` passed; reload succeeded and Nginx stayed active.
- Normal homepage: HTTP 200, 80 distinct game links in server-rendered HTML,
  and `Tüm Oyunlar` present. This verifies the existing cached anonymous page,
  not database health or authenticated playability.
- Claimed ClaudeBot: HTTP 403, no upstream request in the dedicated log.
- Bounded test of 45 short-query search requests: 31 HTTP 200 and 14 HTTP 429.
  Each supplied a different spoofed forwarding/CF client IP header; this did not
  bypass the actual source-IP bucket. The short query returns before any DB call.
- Existing CSS asset: HTTP 200. `www` redirect: HTTP 301.
- Dedicated JSON log is receiving records. Existing `/var/log/nginx/*.log`
  rotation applies daily with 14 retained files and compression.
- Existing application containers/image and persistent cache have not been replaced.

## Application and database

Supabase plugin still reports `yjoipnnyhpgvzukhrljp` as INACTIVE. The previous
October 1 restore plan requires the owner's clarification before an earlier
restore. The supported deploy script checks live Supabase readiness and worker
health; these checks are not being bypassed. No database migration or grant change
has run. No environment telemetry switch or container restart has run.

PR: https://github.com/huseyinemanet/boloyun.com/pull/19 (draft until restore/deploy).

Initial CI caught existing dependency advisories during `pnpm audit`. The patch
updates Next/eslint-config-next to 16.3.3, sharp to 0.35.4 and the affected PostCSS,
Browserslist, baseline-browser-mapping, fast-uri, js-yaml, Hono and qs resolutions.
No security gate was disabled. Package versions remain pinned in the lockfile.
The updated local audit reports no known vulnerabilities.

The Next.js patched version was checked against the maintainer advisory:
https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4

Local verification on Node 24.21.0 after the dependency update: typecheck, lint,
219/219 tests, production build, bundle budget and cache smoke test passed.
Repeated search API/page, game, missing game and sitemap requests again made
zero database calls. GitHub checks must pass on this final revision before merge;
see PR checks for the current CI result. No application deployment is claimed.
